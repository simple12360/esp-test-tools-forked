check_author:
  stage: post_check
  image: $ESP_DOCS_ENV_IMAGE
  tags:
    - build
  rules:
    - if: $CI_PIPELINE_SOURCE == 'merge_request_event'
    - if: $CI_COMMIT_BRANCH == $CI_DEFAULT_BRANCH
  script:
    - bash $CI_PROJECT_DIR/.gitlab/tools/check_author.sh